<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ptsafe">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_1"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round_1"
        android:supportsRtl="true"
        android:theme="@style/Theme.PTsafe"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".LightBreathingActivity"
            android:exported="true" />
        <activity
            android:name=".BoxBreathingActivity"
            android:exported="true" />
        <activity
            android:name=".HistoricalGraphActivity"
            android:exported="true" />
        <activity
            android:name=".ShowCarriageDetails"
            android:exported="true" />
        <activity
            android:name=".ListCarriageActivity"
            android:exported="true" />
        <activity
            android:name=".CrowdednessReportActivity"
            android:exported="true" />
        <activity
            android:name=".CarriageDetails"
            android:exported="true" />
        <activity
            android:name=".ListTrainActivity"
            android:exported="true" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".ViewStationActivity"
            android:exported="true"
            android:label="@string/title_activity_view_station" />
        <activity
            android:name=".LoginActivity"
            android:exported="true" />
        <activity
            android:name=".ShowCrowdActivity"
            android:exported="true" />
        <activity
            android:name=".AddCommentsActivity"
            android:exported="true" />
        <activity
            android:name=".AddNewsActivity"
            android:exported="true"
            android:label="" />
        <activity
            android:name=".NewsDetails"
            android:exported="true"
            android:label="" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="" />
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:label="">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.android.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>